# Milestone 9 — Rapport stock Excel coloré & import bon de commande

Le support est une propriété de **Bechir Bejaoui**

---

## Objectif du sprint

Connecter **Al Qalam Stock Manager** au monde professionnel Excel via `openpyxl`.
À la fin de ce sprint, l'application peut :

- **Générer** un classeur Excel `.xlsx` multi-feuilles avec colorisation conditionnelle du stock
- **Styliser** les cellules : polices, alignements, bordures, largeurs de colonnes adaptées
- **Fusionner** des cellules pour les titres et les lignes de totaux
- **Insérer** un graphique en barres dans la feuille Statistiques
- **Lire** un bon de commande fournisseur Excel et approvisionner le stock automatiquement
- **Prévisualiser** un fichier Excel avant de l'importer (structure + aperçu des données)

---

## Concepts Python mis en pratique

| Concept | Classe / Fonction | Usage dans Al Qalam |
|---|---|---|
| `Workbook` / `Worksheet` | `openpyxl` | Création classeur multi-feuilles |
| `PatternFill` | `openpyxl.styles` | Colorisation conditionnelle (rouge/orange/vert) |
| `Font` / `Alignment` / `Border` | `openpyxl.styles` | Mise en forme professionnelle |
| `merge_cells()` | `Worksheet` | Titres et totaux sur plusieurs colonnes |
| `column_dimensions` | `Worksheet` | Largeur automatique des colonnes |
| `freeze_panes` | `Worksheet` | Figer la ligne d'en-tête |
| `BarChart` / `Reference` | `openpyxl.chart` | Graphique valeur stock par catégorie |
| `load_workbook()` | `openpyxl` | Lecture bon de commande fournisseur |
| `iter_rows(values_only=True)` | `Worksheet` | Parcours ligne par ligne |
| `get_column_letter()` | `openpyxl.utils` | Conversion index numérique → lettre |

---

## Architecture V9

```
alqalamv9/
├── config.py                    ← v9.0.0 + EXCEL_DIR + 5 constantes couleurs Excel
├── main.py                      ← [V9] docstring mise à jour
├── models/                      ← inchangé V8
├── decorateurs/                 ← inchangé V8
├── metaclasses/                 ← inchangé V8
├── validators/                  ← inchangé V8
├── services/
│   ├── stock_service.py         ← inchangé V8
│   ├── journal_service.py       ← inchangé V8
│   ├── surveillance_service.py  ← inchangé V8
│   ├── log_parser.py            ← inchangé V8
│   ├── csv_service.py           ← inchangé V8
│   └── excel_service.py         ← [V9] NOUVEAU — export rapport + import bon commande
├── ui/
│   ├── app.py                   ← [V9] 8ème onglet "📊 Excel" ajouté
│   └── frames/
│       ├── excel_frame.py       ← [V9] NOUVEAU — UI export/import Excel
│       └── ...                  ← autres frames inchangées
└── data/
    ├── stock.json
    ├── csv/
    └── excel/                   ← [V9] rapports générés + bon de commande démo
        └── bon_commande_fournisseur.xlsx
```

---

## Ce qui a été implémenté

### 1. `config.py` — Nouvelles constantes Excel

```python
EXCEL_DIR             = DATA_DIR / "excel"   # dossier exports Excel

EXCEL_COULEUR_ENTETE  = "1F4E79"  # bleu foncé — en-têtes
EXCEL_COULEUR_RUPTURE = "FDECEA"  # rouge pâle — stock = 0
EXCEL_COULEUR_ALERTE  = "FEF9E7"  # orange pâle — 0 < stock ≤ seuil
EXCEL_COULEUR_OK      = "EAFAF1"  # vert pâle   — stock > seuil
EXCEL_COULEUR_TOTAL   = "EBF5FB"  # bleu pâle   — lignes totaux
```

---

### 2. `services/excel_service.py` — ExcelService

#### Export : rapport de stock multi-feuilles

```python
service = ExcelService(stock_service)
chemin  = service.exporter_rapport_stock()   # → Path du .xlsx créé
```

**Feuille 1 — "📦 Catalogue"**
- Toutes les colonnes produit : ref, nom, catégorie, qté, seuil, prix, valeur, marge, statut
- Colorisation conditionnelle ligne par ligne selon l'état du stock
- Ligne de total fusionnée (7 colonnes) avec valeur totale du stock
- En-têtes figés avec `freeze_panes`

**Feuille 2 — "⚠️ Ruptures"**
- Filtre automatique sur les produits avec `qte ≤ seuil_min`
- Message "Aucune rupture" si le stock est en parfait état

**Feuille 3 — "📊 Statistiques"**
- Agrégation par catégorie : nb produits, valeur stock, marge totale, ruptures, alertes
- Ligne TOTAL GÉNÉRAL
- Graphique en barres verticales (valeur de stock par catégorie)

#### Import : bon de commande fournisseur

```python
rapport = service.importer_bon_commande("bon_commande.xlsx")
# → {"traites": 5, "erreurs": [], "total_lues": 6, "total_qte": 90}
```

- Lit la **première feuille** du classeur
- **Ligne 1** : en-têtes (doit contenir `ref` et `qte`, `note` optionnel)
- Pour chaque ligne valide : appelle `stock_service.entree_stock(ref, qte, note)`
- Lignes invalides (ref inconnue, qté négative) → rapport d'erreurs sans interruption

#### Prévisualisation

```python
apercu = service.previsualiser_excel("fichier.xlsx", nb_lignes=5)
# → {"colonnes": [...], "lignes": [...], "valide": True, "feuilles": [...]}
```

---

### 3. `ui/frames/excel_frame.py` — ExcelFrame

**Section Export (colonne gauche)**
- Légende colorisation : rouge / orange / vert avec explication
- Bouton "📊 Générer rapport Excel" → dialogue `asksaveasfilename` → génération
- Bouton "📂 Ouvrir le dernier rapport" → `os.startfile()` (Windows)
- Journal des exports horodatés
- Compteur stock courant mis à jour par polling (5 s)

**Section Import (colonne droite)**
- Sélection fichier via `filedialog.askopenfilename`
- Prévisualisation automatique dans un `Treeview` (5 premières lignes)
- Détection des feuilles (`nb_feuilles`, noms)
- Validation structure (colonnes `ref` et `qte` requises)
- Bouton "⬇️ Importer le bon de commande" → rapport détaillé

---

### 4. `ui/app.py` — Intégration V9

Modifications apportées :
- Instanciation et ajout de `ExcelFrame` dans l'onglet `"📊 Excel"`
- `_post_operation()` : rafraîchissement de `excel_frame`
- `_quitter()` : arrêt du polling `excel_frame.arreter_polling()`
- Pied de page mis à jour : "V9 Excel Rapport & Bon de commande"

---

## Fichier démo fourni

`data/excel/bon_commande_fournisseur.xlsx`

| ref | qte | note |
|---|---|---|
| CRAY-002 | 20 | Réassort crayons HB — fournisseur Bic |
| STYL-002 | 15 | Réassort stylos encre rouge |
| PAP-A3 | 10 | Réassort ramettes A3 |
| CAR-001 | 8 | Réassort carnets lignes |
| GOM-001 | 25 | Réassort gommes blanches |
| CIS-001 | 12 | Réassort ciseaux bureau |

Ce fichier peut être importé directement via l'onglet "📊 Excel" pour tester l'approvisionnement automatique du stock.

---

## Points clés openpyxl à retenir

### Colorisation conditionnelle (côté Python, pas formule Excel)

```python
# On choisit la couleur AVANT d'écrire la cellule, selon la logique métier
if produit.qte == 0:
    couleur = EXCEL_COULEUR_RUPTURE   # rouge pâle
elif produit.qte <= produit.seuil_min:
    couleur = EXCEL_COULEUR_ALERTE    # orange pâle
else:
    couleur = EXCEL_COULEUR_OK        # vert pâle

cellule.fill = PatternFill(fill_type="solid", fgColor=couleur)
```

### Graphique en barres

```python
from openpyxl.chart import BarChart, Reference

chart = BarChart()
chart.type  = "col"            # barres verticales
chart.title = "Valeur par catégorie"

# Données : colonne C, lignes 2 → 2+nb_cats (inclut en-tête pour légende)
data = Reference(ws, min_col=3, max_col=3, min_row=2, max_row=2 + nb_cats)
cats = Reference(ws, min_col=1, min_row=3, max_row=2 + nb_cats)

chart.add_data(data, titles_from_data=True)
chart.set_categories(cats)
ws.add_chart(chart, "A10")   # position dans la feuille
```

### Lecture d'un Excel externe

```python
wb = openpyxl.load_workbook("fichier.xlsx", data_only=True)
ws = wb.active

entetes = [str(c.value).strip().lower() for c in ws[1]]

for ligne in ws.iter_rows(min_row=2, values_only=True):
    if all(v is None for v in ligne):
        continue          # ignorer lignes vides
    ref = ligne[entetes.index("ref")]
    qte = int(float(str(ligne[entetes.index("qte")])))
```

---

## Test de lancement

```bash
# Installer openpyxl si pas encore fait
py -3 -m pip install openpyxl

# Lancer l'application V9
cd alqalamv9
py -3 main.py
```

Vérifier :
1. L'onglet "📊 Excel" est présent (8ème onglet)
2. Clic "📊 Générer rapport Excel" → choisir un emplacement → fichier `.xlsx` créé
3. Clic "📂 Ouvrir le dernier rapport" → Excel s'ouvre avec 3 feuilles colorisées
4. Section Import → "📁 Parcourir" → sélectionner `data/excel/bon_commande_fournisseur.xlsx`
5. Prévisualisation affichée → "⬇️ Importer" → stock approvisionné

---

## Différences V8 → V9

| Aspect | V8 — CSV | V9 — Excel |
|---|---|---|
| Module | `csv` (stdlib) | `openpyxl` (pip) |
| Format | `.csv` (texte brut) | `.xlsx` (binaire structuré) |
| Styles | Aucun | Polices, couleurs, bordures |
| Graphiques | Impossible | `BarChart` intégré |
| Feuilles | Une seule | Multi-feuilles |
| Types de données | Tout en chaîne | Typés : int, float, date, str |
| Ouverture | Éditeur texte / Excel | Excel / LibreOffice uniquement |
